header
    h1.name= name
    p.email Email:  #{email}
    p.github Github: 
        a(href=`${github.url}` target='_blank') #{github.username}
    p.phone 📞 #{phone}
        
    hr
    h2.title #{goal}
    hr