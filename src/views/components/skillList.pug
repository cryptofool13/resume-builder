h3 Tech Skills:
ul 
    each skill in skillList
        li #{skill.skill}: #{skill.exp}
h3 Frameworks:
ul
    each fw in frameworks
        li #{fw.name}
            p #{fw.exp}
            if fw.projects
                ul
                    each project in fw.projects
                        li
                            a(href=`${project.url}` target='_blank') #{project.name}